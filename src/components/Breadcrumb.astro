---
export interface Crumb {
  href?: string;
  label: string;
}

export interface Props {
  crumbs: Crumb[];
}

const { crumbs } = Astro.props;
---
<nav aria-label="breadcrumb" class="mb-6 flex items-center text-scale--1 text-text-note">
  {crumbs.map((crumb, i) => (
    <div
      class:list={[
        'flex items-center',
        i < crumbs.length - 1 && "after:content-['/'] after:mx-2 after:text-text-note",
      ]}
    >
      {crumb.href ? (
        <a href={crumb.href} class="underline hover:decoration-2 text-text-body">
          {crumb.label}
        </a>
      ) : (
        <span aria-current="page" class="font-medium text-text-body">
          {crumb.label}
        </span>
      )}
    </div>
  ))}
</nav>
